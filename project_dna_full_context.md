# Project DNA: Architecture & Context (2026-03-24)
*Этот файл содержит полный слепок контекста системы на момент завершения этапа Semantic Auto-Routing. Прикрепи этот файл к новому чату для мгновенного погружения агента в курс дела.*

## 1. Текущий статус и архитектура (Что уже реализовано)
Project DNA — это система управления знаниями, выступающая в качестве умной прослойки между интерфейсами общения с LLM (Open WebUI, кастомные расширения браузера, локальные UI) и самой нейросетью.

*   **API-Шлюз (Router) с ротацией ключей**: Ядро бэкенда (`router.py`) проксирует запросы на любые модели, используя LiteLLM (с возможностью Fallback-сценариев). Безупречно поддерживает потоковую (event-stream), синхронную генерацию текста, а также генерацию и озвучку(Chatterbox TTS).
*   **Semantic Auto-Router (`semantic_router.py`)**: Внедрена Zero-Shot классификация на базе молниеносной модели `gemini-2.5-flash-lite`. Она перехватывает запросы из Open WebUI на подлете, анализирует смысл (если юзер явно не указал проект) и складывает выводы в нужную папку в БД.
*   **Zero-Latency Caching**: Роутер считает `md5`-хэш самого первого корневого сообщения в диалоге и кэширует за ним `slug` проекта. Это гарантирует 0 мс задержки на любые последующие сообщения в рамках одной ветки диалога.
*   **Agentic Interceptor**: Система-ловушка в `router.py`. Если Semantic Router выдает `UNKNOWN` (сомневается, куда отнести диалог), система перехватывает стрим и возвращает фейковый ответ *прямо в чат Open WebUI* со списком проектов на выбор (1. MLOps, 2. test и т.д.). Затем парсит ответ юзера цифрой, **удаляет эти технические сообщения из истории**, сохраняет выбор и делает проброс к реальному LLM.
*   **Frontend Dashboard (`dashboard/index.html`)**: Нативная панель администрирования.
    *   Кнопки `Delete Project`, просмотр контекстов.
    *   Модуль пакетного редактирования: Чекбоксы на записях, возможности **Batch Delete** (удаления из базы) и **Batch Move** (переноса ошибочно залетевших сообщений в другой проект по щелчку — с помощью параллельных `PUT` запросов).
*   **Observability Stack (G10)**: Интегрирован стек мониторинга инфраструктуры: Telegraf парсит `docker.sock` (обходя ограничения cgroups v2), передает метрики в Prometheus, которые визуализируются в Grafana. Настроены автоматические алерты (SMTP) на перерасход ресурсов.
*   **Disaster Recovery Pipeline (G9)**: Реализована стратегия бэкапирования и восстановления уровня Enterprise (`backup.sh` / `restore.sh`). Включает снятие `pg_dump` "на лету" (Zero-Downtime), а также физический захват (через `tar`) томов `deploy_pg_data`, `qdrant_data`, `minio_data` и `n8n_data`. Проведен успешный боевой краш-тест (Game Day) с удалением всех томов (`docker compose down -v`) и полным восстановлением из архива, включая применение "Instant Restore to VMware" (клонирования VM) через **Synology Active Backup for Business** для спасательной хирургии без сетевых конфликтов.

## 2. Локации файлов
*   **Back-End (Remote Server - Ubuntu)**: `routing/router.py`, `routing/db.py`, `routing/semantic_router.py`, `routing/dna_capture_middleware.py`.
*   **Front-End (Local Windows)**: `c:\Projects\Business\Web_projects_NEW\dashboard\index.html` (Загружается на сервер вручную через VIM).
*   **Google Chrome Extension**: `c:\Projects\Business\Web_projects_NEW\project-dna-extension\` 
    *   `background.js` (Отвечает за перехват запросов Gemini и AI Studio + ручное выделение).

## 3. Ближайший план работы (Action Plan для нового чата)
**Phase 1: G12 - Система Резервирования (Redundancy / Failover)**
Создание "Теплого Зеркала" (Warm Standby) на базе XPenology NAS:
*   [ ] Развернуть Docker-контейнеры на XPenology.
*   [ ] Поднять Nginx в качестве Failover-балансировщика.
*   [ ] Реализовать репликацию минимального стека (LLM Router, Qdrant/MinIO replicas) для обеспечения High Availability при отказе основного Ubuntu-сервера.

**Phase 2: G11 - Интеграция Контекста (Context Integration)**
*   [ ] Разработать CLI-утилиту или Middleware для инъекции диалогового контекста внутрь Project DNA (RAG).
*   [ ] Обеспечить прозрачный проброс исторических данных (Project → General Context) в текущую генерацию (prompt injection).

## 4. Идеи для резюме MLOps Инженера (ML Engineering / AI Integration)
Достижения в рамках этого микросервиса, которые стоит добавить в CV:
*   Разработал *Intelligent AI-Gateway Router* на основе FastAPI, управляющий балансировкой и ротацией API-ключей для LLM.
*   Спроектировал и внедрил *Semantic Auto-Rouing Pipeline* с использованием Zero-Shot классификации легковесных LLM, обеспечивающий автоматическую категоризацию RAG-записей. Задержка последующих вызовов сокращена до 0мс за счет MD5-кэширования ветки диалога.
*   Реализовал архитектурный паттерн *Agentic Interceptor*: создал машину состояний поверх SSE-стримов, изолирующую пользователя от реальной нейросети при неопределенных интентах для создания интерактивных prompt-диалогов прямо внутри оболочек вроде Open WebUI.
*   Создал асинхронный Fullstack-Дашборд управления контекстами (`PostgreSQL` + `asyncio`) для мониторинга баз знаний (RAG).
